// Runtime configuration. The container entrypoint overwrites this file at
// start from the deployment's values. Empty here so development falls back to
// the build-time VITE_* variables.
window.__AXIOMA_CONFIG__ = {};
